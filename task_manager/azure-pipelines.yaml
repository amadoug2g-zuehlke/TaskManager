trigger:
  - main

pool:
  vmImage: 'macOS-latest'

steps:
  - task: Hey24sheep.flutter.flutter-install.FlutterInstall@0
    inputs:
      mode: 'auto'
      channel: 'stable'
      version: 'latest'
  - task: Hey24sheep.flutter.flutter-install.FlutterBuild@0
    displayName: 'Build application'
    inputs:
      target: 'apk'
      projectDirectory: './task_manager'
